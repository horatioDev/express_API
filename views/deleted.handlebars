<h1 class="text-center mb-3">{{ title }}</h1>

<div class="container">
  <h4>Member:</h4>
  <ul class="list-group">
    <li class="list-group-item d-flex justify-content-between align-items-center">
      {{!-- {{member.id}} {{deletedMember.id}}: {{member.name }} {{deletedMember.name}} - {{member.email }}
      {{deletedMember.email}} --}}
      <p class="message mt-4">{{ message }}</p>
    </li>
  </ul>
  <!-- BUTTONS -->

  <a href="/api/members" class="btn btn-light mt-2">View API</a>
  <a href="/" class="btn btn-primary mt-2">Home</a>
</div>